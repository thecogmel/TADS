var arr = [1, 2, 3, 4, 5, 8, 9];

const newArr = arr.map (item => item * 2);

console.log(newArr);

const teste = () => ({nome: 'Erick'})

console.log (teste());

/* arrow function   (parametro => algoritmo de retorno unico) */